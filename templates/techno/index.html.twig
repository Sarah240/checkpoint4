   
{% extends "base.html.twig" %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-12 col-md-10 col-lg-8 col-xl-6">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h1 class="h3">Liste des tecnhologies</h1>
                    <a href="{{ path("techno_create") }}" class="btn btn-primary"><i class="fas fa-plus"></i></a>
                </div>
                <table class="table table-striped table-hovered mb-0">
                    <thead>
                        <tr>
                            <th class="text-center text-uppercase">Id</th>
                            <th class="text-center text-uppercase">Nom</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for techno in technos %}
                            <tr>
                                <td class="text-center align-middle">{{ techno.id }}</td>
                                <td class="text-center align-middle">{{ techno.name }}</td>
                                 <td class="text-center align-middle">
                                    <a href="{{ path("techno_update", { id: techno.id }) }}" class="btn btn-warning"><i class="fas fa-edit"></i></a>
                                    <a href="{{ path("techno_delete", { id: techno.id }) }}" class="btn btn-danger"><i class="fas fa-trash"></i></a>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="4" class="text-center align-middle">Aucune technologie</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
